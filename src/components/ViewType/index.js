import React from 'react';

export default function () {
    return (
        <div className="viewType__wrapper">
            <button>Shorthand view</button>
            <button>Detailed gist view</button>
        </div>
    )
}